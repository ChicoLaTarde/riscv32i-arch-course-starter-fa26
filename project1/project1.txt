
Author: David
[Q0]: No, both are U32, and their complete product requires up to 64-bit. Any value above 31 are discarded if the product overflows.
[Q1]: Avarage Operation could have been added. Where it shifts when dividing by 2,
[Q2]: Hardware mul will make multiplication much faster because the processor could perform the operation directly instead of software looping and shifting. A downside is that the CPU requires more complex hardware, chip area, and power consumption.
[Q3]:One difference is that RV32I has 32 general-purpose registers, while 32-bit IA-32 has only eight general-purpose registers. secondly, RV32I is a ld/str architecture, meaning arithmetics instructions operate on registers and separate ld/str instructions access memory. IA-32 can often operate directly on operands. Thirdly, RV32I instructions have a fixed length of 32-bit, while IA-32 instructions have variable lengths. which makes IA-32 to be more flexible when it comes to more complicated instructions encodings.
[Q4]: C provides abstractions such as variables, loops, functions, arrays, and types without requiring the programmer to directly manage processor instructions and registers. RISC-V ASM is low-level language where the programmer directly has machine instructions, registers, branches and memory operations. C is a lot more friendly because it is easier to read and maintain and because of compilers, it is flexible between architectures. While ASM is useful when directly controlling hardware such as when debuging or low-level optimization.
[Q5]: A compiler translates C code into assembly or another lower-level representation. An assembler then converts assembly into machine code, and a linker combines the object files and required libraries into an executable. The compiler can perform optimizations such as removing unnecessary instructions, improving loops, and allocating registers efficiently. The design and optimization abilities of these tools can therefore affect the performance and size of the final program.
[Q6]: Programs that perform multiplication frequently could run significantly faster with a hardware multiplication instruction because they would require fewer instructions. But on the other hand, Programs that rarely multiply would receive little to no performance improvement. But in the hardware there is still a costs that affact the processor as a whole. This could be chip area, power consumption, and design complexity.
[Q7]: The program performs an integer Fahrenheit-to-Celsius conversion. If the input in a0 is F, it first computes F - 32, multiplies that value by 5 using a shift and addition, and then effectively divides by 9 using repeated subtraction. Therefore, it implements C = 5(F - 32) / 9 using integer arithmetic.
[Q8]: To produce the opposite effect, I would modify the program to convert Celsius to Fahrenheit. The inverse equation is F = 9C / 5 + 32. The assembly could multiply the input by 9 using shifts and additions, divide the result by 5 using repeated subtraction, and then add 32 to the result before returning it in a0.
[Q9]: Adding a hardware integer division unit would make this program faster. The current code implements division by repeatedly subtracting 9 in a loop, which can require many instructions. A hardware divider could calculate the quotient directly in far fewer instructions.